# Product codecs (wl-display)

**Wire:** `enc=0` **`raw_rgb565` only** (W21).

Compression experiments (`delta_rle_v1`, benches, Huffman/YUV ideas) live in the sibling lab:

→ **[`../delta-rle-lab`](../../delta-rle-lab)** (`make test` / `make bench` there).

Historical freeze notes and MVP tables were copied into that lab at Step 16.
