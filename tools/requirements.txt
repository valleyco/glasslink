# Host MQTT / demo / scene tools (tools/wd_mqtt.py, wd_demo.py, wd_scene.py).
#   python3 -m venv tools/.venv
#   tools/.venv/bin/pip install -r tools/requirements.txt
paho-mqtt==2.1.0
Pillow>=10
PyYAML>=6
